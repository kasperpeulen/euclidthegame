function encircle(number){
	var encircled = parseInt(number);
	if (encircled > 20)
	{encircled = 9331;}
	else{encircled = encircled + 9311;}
	if (parseInt(number) <= minlevel{{p.number}}){
	encircled = "<span id=\"medalgold\">&#"+encircled+"</span>";
	}
	else{	encircled = "<span id=\"medal\">&#"+encircled+"</span>";}
	return encircled;
}

function encircle2(number){
	var encircled = parseInt(number);
	if (encircled > 20)
	{encircled = 9331;}
	else{encircled = encircled + 9311;}

	if (parseInt(number) <= minlevel{{p.number}}p){
	encircled = "<span id=\"medalgold\">&#"+encircled+"</span>";
	}
	else{	encircled = "<span id=\"medal\">&#"+encircled+"</span>";}
	return encircled;
}